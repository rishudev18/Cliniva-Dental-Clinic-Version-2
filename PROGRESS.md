# Progress Log

## Current session
10 (Home page premium visual pass — planning + reduced-motion fix)

## Current step
Plan for the home-page visual pass written and approved (8 items), but **held pending an updated SPEC.md §5.2/§5.3 token section from the client**. Nothing from that plan is implemented yet. Shipped this session: a confirmed `prefers-reduced-motion` accessibility fix, and 30 baseline screenshots for the pending token change.

## Session map
(check off when the whole session is complete and verified)

- [x] Session 1 — Setup, content layer, primitives (SPEC.md §12 Steps 1–3) — completed 2026-07-18 (Steps 2–3 caught up in the Session 2 sitting, per user approval)
- [x] Session 2 — Layout, cards/blocks, Cost Clarity Table (Steps 4–6) — completed 2026-07-18
- [x] Session 3 — Home page, Services index + detail template (Steps 7–8) — completed 2026-07-18
- [x] Session 4 — About, Pricing, Contact, Book, Thank-you (Steps 9–10) — completed 2026-07-18
- [x] Session 5 — SEO layer, a11y/perf pass, final acceptance check (Steps 11–13) — completed 2026-07-18
- [x] Session 6 — Nav revision: restructured nav, /smile-gallery, content/gallery.ts (Step 14) — completed 2026-07-19
- [x] Session 7 — Hero headline copy change — completed 2026-07-19
- [x] Session 8 — Home page About us teaser + section reorder — completed 2026-07-19
- [x] Session 9 — Hero micro-trust row update — completed 2026-07-19
- [ ] Session 10 — Home page premium visual pass (plan approved; blocked on client token spec) — reduced-motion fix shipped 2026-07-28

## Step log
Append one entry per completed step. Never delete old entries — this file is the project's memory across sessions.

### Template
```
### Step N — <name> — <date>
- What was built:
- Decisions made / deviations from SPEC.md (and why):
- Verified: (e.g. view-source checked, Lighthouse score, keyboard nav tested)
- Commit: <message or hash>
- Open questions for next session:
```

---

(Entries start below this line)

### Step 1 — Setup — 2026-07-18
- What was built: git repo initialised; manual Next.js 15.5 + TypeScript 5.9 (strict) + Tailwind 3.4 scaffold (`package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`); all §5.1 color tokens + §5.3 shadows/radii in `tailwind.config.ts`; §5.2 fonts (Newsreader 400/500, Public Sans 400/500/600, IBM Plex Mono 400/500) via `next/font/google` with CSS variables in `app/layout.tsx` (`lang="en-IN"`); §15 folder structure (`components/{layout,ui,blocks,forms}`, `content`, `lib`, `public/{images,icons}` with .gitkeep); temporary placeholder `app/page.tsx` (replaced in Step 7). All §2.2 deps installed up front: next, react, react-dom, lucide-react, @next/third-parties, zod, resend.
- Decisions made / deviations from SPEC.md (and why):
  - Manual scaffold instead of `create-next-app` — the CLI refuses non-empty dirs and its template now ships Tailwind v4 (no `tailwind.config.ts`), which conflicts with §2/§5.1. Tailwind pinned to v3.4 so tokens live in `tailwind.config.ts` as specified.
  - TypeScript pinned to ^5.9 — npm resolved typescript@7 (native), which Next 15.5 cannot use to load `next.config.ts` (build failed until pinned).
  - No ESLint/Prettier — not on the §2.2 allowed list.
  - The spec file is named `dental-clinic-website-spec.md`, not `SPEC.md` as CLAUDE.md says — treated as the same document.
  - `npm audit`: 3 moderate advisories, all a transitive `postcss` pinned inside `next` itself; no fix short of a breaking downgrade — left as-is.
- Verified: `npm run build` clean, zero TS errors under strict mode; server-rendered HTML fetched raw (no JS) contains all page copy; computed styles in browser confirm porcelain/clinic/rinse hex values, 999px/12px radii, shadow-card, and all three fonts loaded; grep confirms no hex outside `tailwind.config.ts` and zero `"use client"` in the codebase; browser console clean.
- Commit: Step 1 — Setup: Next.js + TS + Tailwind scaffold, design tokens, fonts, folder structure
- Open questions for next session: none blocking. §13 TODOs (email, established year, reg. numbers, prices, geo/embed URL) will get plausible placeholders in Step 2 per spec.

### Step 2 — Content layer — 2026-07-18
- What was built: all six §4 content files plus one addition. `clinic.ts` (§13 confirmed data; typed `Clinic`), `services.ts` (all 12 services with full copy: oneLiner, problem, 2-para description, 3–5 process steps, fact-strip fields, price range + priceNote, aftercare, 3–4 FAQs each; plus `categoryLabels`, `categoryOrder`, `primaryServices`, `getService`), `doctors.ts` (4 doctors, 2-para bios), `testimonials.ts` (7 entries, ≤180 chars), `faqs.ts` (10, covering every §4.5 required topic), `trust.ts` (exactly 4 stats), and `pricing.ts` (new file — Cost Clarity Table copy: rangeNote, included/extra columns, payment/insurance/no-upsell notes).
- Decisions made / deviations from SPEC.md (and why):
  - Added `priceNote: string` to the `Service` type — §5.5 requires "what determines the range" per table row but §4.2's type had no field for it.
  - Added `content/pricing.ts` beyond the six §4 files — §5.5/§7.5 copy (range note, included/extra, payment/insurance/no-upsell) must live in `/content` per hard rule 4 and had no home otherwise.
  - Plausible placeholders (flagged in file comments, to be confirmed before launch): email hello@cliviadental.in, established 2012, geo pin 28.4889/77.0926, maps URLs, doctor reg. numbers (A-XXXXX Haryana State Dental Council pattern), all 12 price ranges, social links, testimonial quotes.
- Verified: `npm run build` clean, zero TS errors strict; grep of /content confirms zero exclamation marks, zero banned phrases (§16: state-of-the-art, world-class, painless, we care, your journey), no lorem ipsum, no ⟨TODO⟩ markers, no hex values. All §4.5 mandatory FAQ topics present; trust.ts has exactly 4 entries; testimonials within 180 chars.
- Commit: Step 2 — Content layer: all /content data files with full placeholder copy
- Open questions for next session: confirm §13 placeholder values before launch (email, year, geo, reg. numbers, prices, reviews).

### Step 3 — Primitives — 2026-07-18
- What was built: `components/ui/Button.tsx` (primary/secondary/ghost × sm/md/lg, renders Next `Link` for internal hrefs, `<a rel="noopener">` for external, `<button>` otherwise; all sizes keep §10's 44px min touch target), `components/ui/SectionHeading.tsx` (mono eyebrow + h1/h2 + optional lead, left/center align), `components/ui/Container.tsx` (§5.3 container), `lib/utils.ts` (`cn`, `formatINR`, `formatINRRange`, `whatsappUrl` with both §8.3 templates), typography base styles in `globals.css` (§5.2 h1/h2/h3 clamp scale, body leading 1.65, global §10 focus ring 2px clinic offset 2, §5.4 prefers-reduced-motion kill switch), tailwind.config.ts extensions (default border = graphite 18%, `text-body-l`/`text-eyebrow` sizes, `duration-320`), and the temporary `/styleguide` route rendering every token, type size, button variant/size/state, shadow, and radius.
- Decisions made / deviations from SPEC.md (and why):
  - Button hover states chosen within tokens: primary fills scrub, secondary re-inks to clinic, ghost underlines + inks scrub (clinic stays interactive-only, rinse never carries text).
  - `/styleguide` displays the 9 token hex strings as text labels for review — same tokens, no new colors; the whole route is deleted before shipping per §12.
  - `whatsappUrl` builds §8.3 message templates from `clinic.name` in `lib` — functional strings specified by the spec, not page copy.
- Verified: `npm run build` clean (5 routes, zero TS errors); /styleguide checked in browser via computed styles — Newsreader h1 (60px/500/-0.02em), IBM Plex Mono eyebrow (12px/0.12em/uppercase), primary button bg rgb(28,123,168), radius 999px, min-height 44px, ease cubic-bezier(0.2,0,0,1), default border rgba(92,110,120,0.18), body leading 1.65; compiled CSS contains `:focus-visible { outline: rgb(28,123,168) solid 2px; offset 2px }` and the prefers-reduced-motion override; console clean; zero `"use client"` in repo.
- Commit: Step 3 — Primitives: Button, SectionHeading, Container, type base styles, /styleguide
- Open questions for next session: none.

### Step 4 — Layout — 2026-07-18
- What was built: `components/layout/` — `Header` (server; sticky enamel bar, §3.2 desktop nav with CSS-only Services dropdown via group-hover/group-focus-within listing the 6 primary treatments, mobile phone icon + hamburger), `MobileNav` (client, permitted; full-screen overlay with body scroll lock, Esc + route-change close, focus trap, focus return to trigger), `Footer` (server; scrub bg, 4 §3.2 columns — clinic/positioning/address/directions, 6 treatment links, clinic pages, hours table + phone/WhatsApp/email — plus © bottom bar with nofollow credit link), `StickyMobileCta` (client, permitted; appears past 400px scroll, Call + WhatsApp halves with §8.3 prefilled deep link), `SkipLink`. All wired into `app/layout.tsx` with `#main-content` focus target.
- Decisions made / deviations from SPEC.md (and why):
  - Header "border after 8px scroll" done with a CSS scroll-driven animation (`animation-timeline: scroll()`) instead of JS — hard rule 1 forbids a client Header; unsupported browsers just always show the border (graceful).
  - Nav labels (Services/Pricing/About/Contact/Book appointment) and footer column headers live in the layout components — they are §3.2 navigation structure fixed by the spec, not page copy; all names/links/copy still come from /content.
  - Footer credit "Website by Rishu Kumar" with placeholder URL rishukumar.dev (spec's ⟨Your Name⟩) — confirm the real link.
  - Learned: don't run `npm run build` while the dev server shares `.next` — it corrupts the dev server's chunks (hit a 500; fixed by clearing .next and restarting). Typecheck with `tsc --noEmit` mid-session instead.
- Verified: production build clean before dev restart; raw no-JS server HTML contains all header/footer copy (name, hours, phone, treatment links, skip link, credit); browser checks at 375px — hamburger visible/desktop nav hidden, overlay opens with 12 links, body scroll locks, aria-expanded toggles, focus moves into overlay, Esc closes + unlocks + returns focus, sticky CTA hidden at top and shows both halves after 400px with correctly encoded wa.me link; at 1280px — dropdown hidden by default, visible on keyboard focus, hidden on blur, lists 6 treatments + view-all; header sticky with header-border-in scroll animation active; console clean; `"use client"` in exactly MobileNav + StickyMobileCta; no hex outside tailwind.config.ts (+ documented styleguide labels).
- Commit: Step 4 — Layout: Header, MobileNav, Footer, StickyMobileCta, SkipLink
- Open questions for next session: /book needs its minimal-header variant when Step 10 builds that page.

### Step 5 — Cards and blocks — 2026-07-18
- What was built: `components/blocks/` — `ServiceCard` (icon in rinse circle from a lucide name→component map, name, oneLiner, price-from in mono, arrow link; whole card is one link with §5.4 hover lift), `DoctorCard` (compact/full variants; 4:5 labelled portrait placeholder per §13 — initials on rinse, "Portrait to come" — name in Newsreader, qualifications + reg. no. in mono, speciality chips, experience; full adds bio + languages), `TestimonialCard` (initials avatar, treatment tag, StarRating, quote, source badge), `TrustBar` (4 mono stats, dividers on md+), `FaqAccordion` (client, permitted; answers always in DOM, collapsed via CSS grid-template-rows 0fr→1fr at duration-320, aria-expanded/aria-controls/role=region wired, multiple items can be open, first open by default), `MapEmbed` (lazy iframe over a static rinse fallback block with address — labelled placeholder since no map image supplied — plus Get directions button). `components/ui/` — `Chip` (rinse fill, scrub text), `StarRating` (scrub-filled stars, role=img aria-label), `Breadcrumbs` (visible trail + server-rendered BreadcrumbList JSON-LD). `lib/jsonld.ts` — `siteUrl` (placeholder domain cliviadental.in) + `breadcrumbListJsonLd`. Styleguide extended to render all of them with real content data.
- Decisions made / deviations from SPEC.md (and why):
  - Star color is scrub (semantic tokens are states-only per §5.1, clinic is interactive-only — scrub is the only compliant ink).
  - MapEmbed fallback is a styled labelled block, not a static image — no map image supplied yet (§13 photos TODO).
  - `lib/jsonld.ts` uses placeholder domain `https://cliviadental.in` for absolute BreadcrumbList URLs — §13 domain unconfirmed.
- Verified: `tsc --noEmit` clean; raw no-JS HTML of /styleguide contains collapsed FAQ answer text, BreadcrumbList JSON-LD, prices/badges (React `<!-- -->` text-node splits accounted for), portrait placeholder, map iframe with loading="lazy" and descriptive title; browser checks — accordion aria-expanded toggles per item, JSON-LD parses as BreadcrumbList, star rating labelled.
  - IMPORTANT verification caveat discovered: the in-app Browser pane runs with `visibilityState: "hidden"` — requestAnimationFrame never fires, so NO CSS transition ever starts there (a plain `height:10px→110px` transition stays stuck too), and screenshots time out. The accordion's grid-rows animation appeared "stuck at 0fr" for this reason only; with `transition:none` forced, the open state measures the full 99px, proving the CSS states are correct. All animation/transition behaviour must be eyeballed in a real browser window; state/DOM/aria checks remain verifiable here.
- Commit: Step 5 — Cards and blocks: ServiceCard, DoctorCard, TestimonialCard, TrustBar, FaqAccordion, MapEmbed, Breadcrumbs
- Open questions for next session: none.

### Step 6 — Signature: CostClarityTable — 2026-07-18
- What was built: `components/blocks/CostClarityTable.tsx`, both variants (§5.5). `full`: honest range note (from `content/pricing.ts`) above; all 12 treatments grouped by category with mono eyebrow headers; each desktop row = name (Public Sans 600, `<th scope=row>`) · price range (IBM Plex Mono, tabular numerals, `₹4,000 – ₹9,000` + unit) · what-determines-the-range (small graphite, from `priceNote`) · Book link (`/book?treatment=slug`, aria-labelled per treatment); below, the What's included / What costs extra columns. `condensed`: the 6 primary treatments, ungrouped, for home//services/service pages. Desktop renders real `<table>`s with sr-only captions/headers; below `md` the same rows render as stacked cards where the mono price (20px) outweighs the name (16px), per §5.5. Both variants added to /styleguide for review.
- Decisions made / deviations from SPEC.md (and why): desktop table + mobile cards are two renderings (`hidden md:block` / `md:hidden`) — a semantic table can't reflow into cards with CSS alone; copy is still single-sourced from /content and both renderings are in the server HTML.
- Verified: `tsc --noEmit` and full production build clean (stopped the dev server first this time); raw no-JS HTML contains all 4 category headers, price notes, units, range note, included/extra lists, and 18 Book links per rendering; browser at 1280px — 5 pricing tables visible, mobile lists hidden, price cells IBM Plex Mono with tabular-nums, row names weight 600; at 375px — tables hidden, cards visible, price 20px > name 16px, no horizontal overflow; console clean. Repo-wide: `"use client"` in exactly MobileNav + StickyMobileCta + FaqAccordion (3 of the 4 permitted; AppointmentForm comes in Step 10); no hex outside tailwind.config.ts (+ documented styleguide swatch labels); content copy greps still clean (no exclamation marks, banned phrases, lorem, or TODO markers).
- Commit: Step 6 — Signature: CostClarityTable, full and condensed variants
- Open questions for next session: Session 3 (Steps 7–8) builds the home page and services pages on top of these blocks. Reminder: the in-app Browser pane cannot render animations or screenshots (hidden renderer) — review animation feel in a normal browser at localhost:3000/styleguide.

### Step 7 — Home page — 2026-07-18
- What was built: `app/page.tsx` — all 10 §7.1 sections in order: (1) Hero — 60/40 split, benefit-statement h1, lead, Book/WhatsApp CTAs, mono micro-trust row, 4:5 labelled interior-photo placeholder (no photo supplied yet, §13), capped at 280px on mobile, whole section wrapped in a 400ms load fade; (2) TrustBar; (3) Services — 6 `ServiceCard`s + "View all treatments" link; (4) Why this clinic — 4 differentiator blocks (sterilisation, tech, pricing, continuity of care) in a 2×2 grid; (5) Cost Clarity condensed — `pricing.rangeNote` as the honest paragraph + `CostClarityTable variant="condensed"` + "See full price list" link; (6) Meet the team — 4 compact `DoctorCard`s in spec order + "More about our doctors" link; (7) Testimonials — 3 `TestimonialCard`s (CSS scroll-snap on mobile, grid on desktop) + aggregate Google rating with a link out; (8) FAQ — 6 questions via `FaqAccordion` + a link to `/contact`; (9) Booking CTA band — new reusable `components/blocks/CtaBand.tsx` (scrub bg, Book/Call/WhatsApp actions, today's hours + emergency note), built reusable because Steps 8–9 need the same band with different copy; (10) Location strip — `MapEmbed` + address/hours table. New content: `content/home.ts` (hero copy, differentiators, team intro, CTA band copy — only the genuinely authored marketing copy; spec-fixed structural headings like "What treatment actually costs" and short section labels stayed inline, matching the Step 6 precedent set by CostClarityTable's "What's included"/"What costs extra"); `clinic.ts` gained a `googleRating` field (value/count/url) since it's clinic-level fact data reusable by the Step 11 JSON-LD `AggregateRating`.
- Decisions made / deviations from SPEC.md (and why):
  - §5.4's scroll-reveal ("fade in + 12px rise, triggered via IntersectionObserver in a small client wrapper") can't be built as specified — hard rule 1 permits `"use client"` only in the 4 named components, and a scroll-reveal wrapper isn't one of them. Built the same effect with a CSS scroll-driven animation (`animation-timeline: view()`, `.reveal` class in `globals.css`) instead, following the exact pattern Step 4 used for the header border. Baked `.reveal` directly into `SectionHeading` so every section heading site-wide gets it automatically; applied it manually to the card-grid wrappers (services, differentiators, team, testimonials). Unsupported browsers show content immediately (no regression); reduced-motion kill switch already in `globals.css` covers it.
    - **Firefox caveat:** as of this build (2026-07-18), Firefox has not shipped `animation-timeline: view()` behind default settings (it has sat behind the `layout.css.scroll-driven-animations.enabled` flag) — confirm current status against caniuse.com before launch, since browser support shifts. The `.reveal` CSS is written specifically so this doesn't matter either way: the bare rule (`opacity: 1; transform: none;`) is declared *before* the `@supports (animation-timeline: view()) { … }` block and is the only rule that applies in a browser where that feature query evaluates false — the scroll-linked, initially-hidden version only exists inside the `@supports` block. No Firefox instance was available to test directly (the in-app Browser pane and Claude in Chrome are both Chromium-based), so this was verified by removing the actual compiled `@supports` rule from the live page's CSSOM via the DevTools console — functionally identical to a browser that never matches the feature query in the first place, since in both cases the block is simply never applied — and re-checking computed styles. Result: all 11 `.reveal` elements on the home page (every section heading and card grid, including several never scrolled into view) computed `opacity: 1`, `transform: none`, `animation-name: none` — fully visible immediately, nothing stuck hidden waiting on an animation that will never fire. No FOUC risk either, since this is resolved by the cascade before first paint, not by JS.
  - Hero photo has no source image yet (§13 TODO) — used a labelled `rinse→enamel` gradient placeholder at the correct 4:5 ratio, same treatment as `DoctorCard`/`MapEmbed`. Flagging explicitly: when a real photo is supplied, it needs to become a `next/image` with `priority` (the spec calls this the LCP element) — that swap hasn't happened, since there is no image asset to point it at.
  - Built `CtaBand` as a shared component rather than a page-only block, since §7.3/§7.4/§7.5 all call for the same banner with page-specific copy — avoids duplicating the layout three more times in Steps 8–9.
  - Location strip's right column shows address + hours only, not a second "Get Directions" button — `MapEmbed` already renders its own Get Directions button under the map (per its Step 5 build and the §6 component table), so a second one in the text column would just duplicate it.
  - Added a small `SectionLink` helper inside `page.tsx` for the three "→" text links (View all treatments / See full price list / More about our doctors) instead of forcing Tailwind `!important` overrides onto the `Button` ghost variant's padding — matches the arrow-link styling `ServiceCard` already uses.
- Verified: `tsc --noEmit` clean; dev server console and server logs clean. Raw no-JS HTML (fetched directly, no browser JS) confirmed: exactly one `<h1>`; heading order is h1 → h2 (8, one per section) → h3 (20: 6 service names + 4 differentiator titles + 4 doctor names + 6 FAQ questions) with no skipped level; hero copy, CTA band copy, and the location strip's address/hours all present verbatim. In-browser (JS on) checks: `.hero-fade` computes `animation-name: hero-fade-in` at 0.4s; `.reveal` computes `animation-timeline: view()`; at 375px — no horizontal overflow, hero image measures exactly 280px tall, testimonials row has `overflow-x: auto` + `scroll-snap-type: x mandatory`; at 1280px — hero splits 624px/416px (60/40), CTA band background is `rgb(11,43,60)` (scrub) with white h2 text, all 6 primary `ServiceCard`s render, all three CTA-band links resolve correctly (`/book`, `tel:+919625722542`, a correctly URL-encoded `wa.me` link) at a 48px touch-target height, aggregate rating renders "Rated 5 out of 5" (rounded from 4.8) with 5 filled stars. Grep confirms `"use client"` still only in the 3 established client files (`AppointmentForm` isn't due until Step 10); no hex outside `tailwind.config.ts` (+ styleguide labels); no `!important`/`!px-` overrides left in the new code; §16 banned-phrase grep clean on all new content.
- Commit: Step 7 — Home page: all 10 sections, CtaBand block, CSS scroll-reveal
- Open questions for next session: Step 8 (services index + detail template, with `generateStaticParams`) will reuse `CtaBand`, `CostClarityTable`, and the `.reveal`/`SectionLink` patterns established here.

### Step 8 — Services index + detail template — 2026-07-18
- What was built: `app/services/page.tsx` (§7.2) — breadcrumb, h1 `Dental treatments in ⟨Locality⟩`, lead paragraph (new `content/servicesIndex.ts`), all 12 treatments grouped by category with mono eyebrow headers, the "Not sure what you need? → Book a consultation" band placed between the general and cosmetic groups, and the condensed Cost Clarity Table at the bottom. `app/services/[slug]/page.tsx` (§7.3) — a single dynamic template statically generated for all 12 slugs via `generateStaticParams`: breadcrumb + h1 (service name + locality) + oneLiner; a 4-item mono fact strip (Duration/Sittings/Pain level/Anaesthesia) on `rinse`; "You may need this if" (the `problem` field, set at `body-l`, in a bordered block); "What it involves" (the `description` paragraphs); "The process" as a numbered vertical list (numbering justified — it's a real sequence); "Cost" (price range in large mono type + the shared `IncludedExtra` block); an aftercare checklist with `Check` icons; service-specific FAQs via `FaqAccordion`; the booking `CtaBand` with the treatment name prefilled into the WhatsApp message and `bookHref="/book?treatment={slug}"`; and up to 3 related treatments from the same category. New shared pieces: `components/blocks/IncludedExtra.tsx` (extracted out of `CostClarityTable` so the detail page's Cost section and `/pricing`'s later build share one source instead of duplicating the included/extra list markup), `content/cta.ts` (the booking band's default copy, moved out of `content/home.ts` since it's shared across pages, not home-specific), and a `cardGridCols(count)` helper in `lib/utils.ts`.
- Decisions made / deviations from SPEC.md (and why):
  - **Card-grid collapse rule (user-directed mid-step):** when a category or the related-treatments set has fewer than 3 items, the grid must show only however many exist — never padded with unrelated treatments or duplicated, and never left with an empty trailing cell. Implemented via `cardGridCols(count)`: 1 item → single column capped at `max-w-sm` (grid alone would otherwise stretch it to the full container width); 2 items → `sm:grid-cols-2` with no `lg:grid-cols-3` (so there's never an empty third cell at any breakpoint); 3+ items → the original `sm:grid-cols-2 lg:grid-cols-3`. Applied to both `/services`' category grids (only `cosmetic`, at 2 items, was affected — `general`/`surgical`/`preventive` all have 3+) and the detail page's related-treatments grid (`braces-and-aligners` and `teeth-whitening`, the only 2-member category, each show exactly 1 related card). A 4-item category (`general`) wrapping to a partial last row in a 3-column grid was left as normal grid behaviour — that's a remainder-of-division wrap, not the "too few items" case the rule targets.
  - Next.js 15 makes `params` a `Promise` for dynamic route pages — `ServiceDetail` is `async` and `await`s it, not a spec deviation but worth noting since the Next.js 15.5 App Router signature differs from the SPEC's implicit Next 14-era assumption.
  - `params.slug` not matching any service calls `notFound()` — not exercised by any real link on the site yet (no free-text slug entry anywhere), but present so a bad/stale URL 404s properly instead of crashing.
  - Neither route exports custom `metadata` yet — deferred to Step 11 per the build order (§9.2 unique titles/descriptions are explicitly that step's scope), so every page under `/services` still inherits the root layout's generic title for now.
- Verified: `tsc --noEmit` clean. In dev: `/services` — all 12 treatments present, grouped correctly, band between general/cosmetic, condensed table at the bottom; `/services/root-canal-treatment` — all 10 sections present and correct (verified against the raw no-JS HTML: 1×`<h1>`, 7×`<h2>`, 14×`<h3>`, no skipped level; `bookHref` includes `?treatment=root-canal-treatment`; `wa.me` link correctly reads "...for Root Canal Treatment..."). Card-grid collapse: `/services/braces-and-aligners`'s related-treatments grid computed `grid-template-columns: 384px` (one column, `max-w-sm`, one card, "Teeth Whitening") — no stretch, no gap; `/services`' cosmetic group computed `grid-template-columns: 532px 532px` (exactly 2 columns, both cards, no empty third cell) while the general group's 4 items still wrap normally into a 3-column/1-column pair of rows. Full production build (`npm run build`, dev server stopped first): compiled clean, zero type errors, all 12 `/services/[slug]/*.html` files generated matching `content/services.ts` slugs exactly (spot-checked 3 more beyond root-canal-treatment — `dental-implants`, `kids-dentistry`, `wisdom-tooth-removal` — for h1 count, fact-strip labels, cost section, and related-treatments section, all present). Dev server restarted after the build and `dental-implants` re-confirmed live with a clean console. Repo-wide: `"use client"` still only in the 3 established files; no hex outside `tailwind.config.ts` (+ styleguide labels); §16 banned-phrase grep clean on all new content.
- Commit: Step 8 — Services index + detail template, with the card-grid collapse rule
- Open questions for next session: Session 4 (Steps 9–10) covers About, Pricing, Contact, Book, and Thank-you. `/pricing` will reuse `IncludedExtra` and the full `CostClarityTable` variant; `/book` introduces `AppointmentForm` (the 4th and last permitted client component) and needs the minimal-header page variant noted back in Step 4.

### Step 9 — About, Pricing, Contact — 2026-07-18
- What was built: `app/(site)/about/page.tsx` (§7.4) — clinic story (breadcrumb, h1, lead, 3 story paragraphs), all 4 doctors as full `DoctorCard`s in spec order (Chawla, Mukherjee, Singh, Mishra) in a grid that's 4-across on desktop and 1-across on mobile, a sterilisation & safety section (4 itemised points — autoclave protocol, single-use items, surface disinfection, documented disposal — each with a rinse-circle icon), a 6-image facility photo grid (labelled placeholders, no photos supplied yet per §13, no lightbox), `TrustBar`, and the booking `CtaBand`. `app/(site)/pricing/page.tsx` (§7.5) — h1 `Treatment costs, listed plainly`, an h2 `The full price list` followed by `CostClarityTable variant="full"` (which itself now renders a 2-paragraph honest intro above the table and `IncludedExtra` below it — see deviations), then three bordered note blocks (Payment & EMI, Insurance & cashless, No upselling) reading straight from `content/pricing.ts`, then `CtaBand`. `app/(site)/contact/page.tsx` (§7.6) — breadcrumb + h1 + lead, then a two-column layout: left is `AppointmentForm` in a bordered card; right is the address block with a `Get directions` button, phone/WhatsApp/email links, the full hours table with today's row highlighted (new `isTodayRow()` helper in `lib/utils.ts`) and a `Today` `Chip`, the emergency note, `MapEmbed`, and a short parking/transit paragraph. New content: `content/about.ts`, `content/contact.ts`; `content/pricing.ts` gained an `introParagraphs: [string, string]` field.
- Decisions made / deviations from SPEC.md (and why):
  - Split `pricing.rangeNote` (the existing short one-liner, still used as-is for the condensed table's lead on the home page and `/services`) from a new `pricing.introParagraphs` (2 paragraphs) used only by `CostClarityTable`'s `full` variant. §7.5 asks for "an honest 2-paragraph intro on why prices are ranges" directly above the full table — rather than duplicating that message as a separate block on the `/pricing` page above a component that *already* prints a one-line version of the same point, the fuller explanation now lives inside the component itself (the one place `variant="full"` is used), so the message appears exactly once, in the right place.
  - `isTodayRow(days)` added to `lib/utils.ts` — `clinic.hours` is currently a single "Monday – Sunday" row, so today always matches it, but the helper is written to correctly parse any single-day or day-range row (including a range that wraps past Sunday) so it keeps working if the clinic ever adds split hours (e.g. weekday vs. Sunday).
  - No separate `WhatsAppButton` component was built despite it being named in SPEC §15's file list — every WhatsApp CTA across the site (Home's hero/CtaBand, Step 4's `StickyMobileCta`, and now `/contact`) has consistently been a plain `<a href={whatsappUrl(...)}>` styled inline, since `lib/utils.ts`'s `whatsappUrl()` already does the one thing that component would do (build the encoded deep link) and a wrapper around a single anchor tag added indirection without saving any code. Flagging this now since it's been the pattern since Step 4 but was never explicitly called out.
- Verified: `tsc --noEmit` clean. Raw no-JS HTML for all three routes: exactly one `<h1>` each; heading order has no skipped levels (checked programmatically — `/about` runs h1→h2→h3→h2→h3→h2→h2, `/pricing` h1→h2→h3×7, `/contact` h1→h2, none skip a level). `/about`: all 4 doctors present in spec order with full bios/languages, all 4 sterilisation points present, all 6 facility labels present, trust stats present. `/pricing`: all 4 category headers present (`&` renders as `&amp;` in raw HTML, checked for that), 24 `Book` links (12 desktop-table + 12 mobile-card renderings), payment/insurance/no-upsell notes all present. `/contact`: map iframe present, directions text present, emergency note present, mailto link present, form fields present in raw HTML. Repo-wide: `"use client"` still only in the 3 established files (`AppointmentForm` added in Step 10, see below); no hex outside `tailwind.config.ts` (+ styleguide labels); grepped every `text-clinic`/`bg-clinic`/`border-clinic` usage across `app/` and `components/` by hand — all are on links, buttons, or an input's `focus:` state, none decorative-only.
- Commit: (combined with Step 10 below)
- Open questions for next session: none.

### Step 10 — Form + Server Action + /book + /thank-you — 2026-07-18
- What was built: `lib/schema.ts` — the shared `appointmentSchema` (zod) plus `dayOptions`/`timeOptions` consts, used identically by the client form and the Server Action. `app/actions/appointment.ts` — the `submitAppointment` Server Action (§8.2): builds `values` from `FormData`, checks the honeypot (silently redirects to `/thank-you` if tripped, sending nothing), checks a per-IP in-memory rate limit (>3 submissions per IP per 10 minutes, via `x-forwarded-for`/`x-real-ip`), validates with `appointmentSchema.safeParse`, sends the email via Resend on success, and redirects to `/thank-you`; on any failure returns a typed `AppointmentFormState` with field errors or a banner message plus the entered values so nothing is lost. `components/forms/FormField.tsx` — label + input slot + hint/error wrapper, wired for `aria-describedby`. `components/forms/AppointmentForm.tsx` — the 4th and last permitted `"use client"` component (§2.1): all 6 §8.1 fields (name, phone, treatment select sourced from `content/services.ts` + "Not sure", preferred day/time selects, message) plus the off-screen `aria-hidden` honeypot, wired to the Server Action via `useActionState`, submit button pending state via `useFormStatus`, and an error banner with the WhatsApp fallback link on failure. `app/book/` — its own `layout.tsx` (minimal header: logo + phone only, slim footer: © line + address, no nav) and `page.tsx` (centred form at `max-w-xl`, 3-item reassurance list from new `content/book.ts`, `?treatment=slug` query param preselects the Treatment field, WhatsApp alternative below the form). `app/(site)/thank-you/page.tsx` — confirmation heading, "What happens next" + 3 numbered steps from new `content/thankYou.ts`, phone + WhatsApp links, `Back to home`, and `metadata.robots = { index: false, follow: false }`.
- Decisions made / deviations from SPEC.md (and why):
  - **Route-group refactor for `/book`'s minimal chrome.** §7.7 needs a different header/footer on `/book` than every other page, but `Header`/`Footer`/`StickyMobileCta` were rendered once in the single root `app/layout.tsx`, which wraps every route unconditionally — there's no way to make one route skip an ancestor layout's chrome without either a client-side pathname check (which would need `"use client"` on `Header`, and hard rule 1 permits that only on 4 named components, none of which is `Header`) or restructuring the layout tree. Moved `Header`/`Footer`/`StickyMobileCta` out of the root layout into a new `app/(site)/layout.tsx` route group, and moved every existing route (`page.tsx`, `services/`, `styleguide/`) into `app/(site)/`. Route groups don't change any URLs — `/`, `/services`, `/services/[slug]`, `/styleguide` all resolve exactly as before, confirmed by the production build's route table. `/book` stays outside the group with its own minimal `layout.tsx`. The root layout now holds only `<html>`/`<body>`, fonts, and `SkipLink` (global on every route including `/book`, since §10 requires it as the first focusable element everywhere). This is a bigger structural change than prior steps' deviations, so flagging it clearly: it was necessary to satisfy §7.7 without breaking hard rule 1, not a stylistic choice.
  - **In-memory rate limiting**, not a database or edge middleware — no rate-limiting library is on the §2.2 allowed list, and a `Map<ip, timestamp[]>` inside the Server Action module is enough to stop casual/scripted abuse on a single Vercel instance. It resets on redeploy/cold start; a persistent store would need a new dependency, which hard rule 5 says to ask about first rather than add unilaterally.
  - **Resend `from` address uses the sandbox `onboarding@resend.dev`**, not a clinic-domain address — the clinic's domain is still an unconfirmed §13 TODO, and Resend requires a verified domain to send from a custom address in production. Flagged in a code comment in `app/actions/appointment.ts`; swap to e.g. `bookings@cliviadental.in` once the domain exists and is verified with Resend. Until `RESEND_API_KEY` is set in the environment (no `.env.local` exists yet — added `.env.example` documenting the one variable needed), every submission correctly falls through to the failure banner + WhatsApp fallback, which is itself a spec-required path and was the one actually exercised in testing (see Verified).
  - Submit button reads "Book appointment", not "Request appointment" or "Submit" — §16 says an action keeps the same name everywhere it appears, and "Book appointment" is already the label used by the header, home CtaBand, and every service page.
  - `/thank-you` kept the standard `Header`/`Footer` (via the `(site)` route group) rather than the minimal `/book` chrome — §7.8 doesn't ask for a focused/distraction-free layout the way §7.7 explicitly does for `/book`, and letting someone browse on from the confirmation page seemed more useful than trapping them.
  - Found and fixed two heading-order bugs while verifying (see Verified) — `/pricing` jumped from `h1` straight to the `<h3>`s inside `IncludedExtra`/the new note blocks with no `h2` in between (fixed by adding an `h2` `SectionHeading` "The full price list" before `CostClarityTable`), and `/thank-you`'s numbered steps were `h3` directly under `h1` with no `h2` (fixed by adding a visible `h2` "What happens next" above the list, which also improves scannability).
- Verified: `tsc --noEmit` and a full production build (dev server stopped first) both clean — `/book` is correctly `ƒ Dynamic` (reads `searchParams` for `?treatment=`), everything else stayed static/SSG, and the `(site)` route group produced no `(site)` segment in any URL. Programmatic heading-order check across all 6 session-4 routes plus the 4 carried-over ones confirmed zero skipped levels after the two fixes above. No-JS raw HTML: `/book` has no `aria-label="Main"` nav and no `aria-label="Treatments"` footer column (minimal chrome confirmed) while every other route has both (standard chrome confirmed); the form's `<form action=...>` attribute is present (proves the Server Action still renders as a real POST target without JS); the honeypot's `name="company"` field and all 13 treatment `<option>`s (12 services + "Not sure") are in the raw HTML. `/thank-you`'s `<meta name="robots" content="noindex, nofollow">` confirmed present. Live in-browser test of the full form flow: (1) submitted an intentionally malformed phone (`98765 43210`, with a space) — got the inline "Enter a valid 10-digit Indian mobile number." error with `aria-invalid="true"`/`aria-describedby="phone-error"` wired, and confirmed via computed values that both `name` and the malformed `phone` were preserved in the fields (uncontrolled inputs, so "keep entered values" holds with no extra code); (2) fixed the phone and resubmitted — since no `RESEND_API_KEY` is configured in this environment, this correctly exercised the email-failure path: `role="alert"` banner appeared with the WhatsApp fallback link, entered values still preserved, and the server log showed the expected caught `Error: RESEND_API_KEY is not configured`; (3) two more submissions (3rd allowed, 4th rejected) confirmed the >3-per-10-minutes rate limit fires with the correct message; (4) inspected the honeypot field's computed styles directly — `position: absolute`, `left: -9999px`, `tabIndex: -1`, inside an `aria-hidden` ancestor, confirming it's invisible and unreachable to a real visitor without needing to trigger an actual bot-path submission. Console clean throughout. Repo-wide: `"use client"` in exactly the 4 permitted files (`MobileNav`, `FaqAccordion`, `StickyMobileCta`, `AppointmentForm`) and nowhere else — hard rule 1 now fully satisfied, all four components in use; no hex outside `tailwind.config.ts` (+ styleguide labels); §16 banned-phrase grep clean on all new content; no `⟨TODO⟩` markers in `/content`.
- Commit: Step 9–10 — Appointment form + Server Action, /book, /thank-you, About, Pricing, Contact
- Open questions for next session: Session 5 (Steps 11–13) is the SEO layer (metadata on every route, JSON-LD, sitemap, robots, OG image, manifest), the a11y/perf pass, and final Section 14 acceptance walkthrough. Outstanding §13 placeholders are unchanged from before (email, established year, geo/embed URL, doctor reg. numbers, all 12 prices, testimonial quotes, social links, domain, footer credit URL) plus two new ones from this session: the clinic's real WhatsApp response-time commitment (`content/book.ts` currently says "within 2 hours" as a placeholder) and the Resend sender address (needs the real domain verified with Resend before launch, see deviations above).

### Step 11 — SEO layer — 2026-07-18
- What was built: `lib/jsonld.ts` gained `dentistJsonLd()` (Dentist/LocalBusiness — address, geo, openingHoursSpecification expanded from `clinic.hours` day-range strings, aggregateRating), `medicalProcedureJsonLd(service)`, `faqPageJsonLd(items)`, and `personJsonLd(doctor)`, alongside the existing `breadcrumbListJsonLd`. New `components/seo/JsonLd.tsx` (a plain Server Component wrapping the `<script type="application/ld+json">` pattern `Breadcrumbs` already used — `Breadcrumbs` now uses it too, no behaviour change). `content/seo.ts` — one `{title, description}` pair per static route, both length-checked by script to land in §9.2's required 50–60 / 140–160 character windows. Every route now exports `metadata` (or `generateMetadata` for `/services/[slug]`, since price/name vary per treatment) with title, description, `alternates.canonical`, and `openGraph` (including `images: ["/opengraph-image"]` — see deviations). Root `app/layout.tsx` gained `metadataBase`, a sitewide `Dentist` JSON-LD script, and a `GoogleAnalytics` component from `@next/third-parties/google` gated behind `process.env.NEXT_PUBLIC_GA_ID` (unset today, so nothing renders yet). `/about` now emits one `Person` JSON-LD script per doctor. `/services/[slug]` emits `MedicalProcedure` + `FAQPage`. Home emits `FAQPage` for its 6 displayed FAQs. `app/sitemap.ts` (7 static routes + 12 service slugs, excludes `/thank-you` and `/styleguide`), `app/robots.ts` (allow all, disallow those same two, references the sitemap), `app/opengraph-image.tsx` (1200×630 `ImageResponse`, scrub background, clinic-token accent bar, clinic name/tagline/locality), `app/icon.svg` (static favicon, scrub square with a "C" initial and a clinic-colored accent dot — placeholder mark, §13 real logo still TODO) and `app/apple-icon.tsx` (180×180 `ImageResponse`, same mark), `app/manifest.ts` (name/short_name/colors from `content/clinic.ts`, one SVG icon entry). Two P1 routes that weren't built yet but are required by the footer/folder structure: `app/(site)/privacy/page.tsx` + `content/privacy.ts` (plain-language policy scoped to what the site actually does — the appointment form's fields, optional GA4, Resend, WhatsApp — flagged for a real legal review before launch the same way other §13 items are flagged), and `app/not-found.tsx` + `content/notFound.ts` (custom 404 with its own minimal self-contained header/footer, `robots: noindex`).
- Decisions made / deviations from SPEC.md (and why):
  - **Built `/privacy` and a custom 404 during the SEO step, not their own step.** Neither is in the numbered §12 build order and CLAUDE.md says work only the assigned steps — but `Footer` has linked to `/privacy` on every single page since Step 4, so it was already a live dead link (a real defect, not scope creep), and `not-found.tsx` is explicitly named in §15's folder structure and §3.1's route table as P1. Both were cheap, self-contained additions; flagging clearly rather than silently expanding scope.
  - **`app/not-found.tsx` has its own inline header/footer, not the standard `Header`/`Footer`.** Next.js only renders a route's ancestor layouts for a `not-found.tsx` boundary — the root `app/not-found.tsx` sits under `app/layout.tsx` only, and `app/(site)/layout.tsx` (which holds `Header`/`Footer`) is a sibling branch, not an ancestor, so it never wraps the global 404. Rather than fight that, the 404 gets its own minimal chrome (logo + phone, slim footer), the same reasoning `/book`'s layout already used.
  - **`og:image` needed an explicit `images: ["/opengraph-image"]` on every route that sets its own `openGraph` object.** Next.js auto-merges the file-convention OG image into a route's resolved metadata only when that route doesn't declare its own `openGraph` field at all (confirmed empirically: `/styleguide`, which has no `openGraph` override, got the image for free; every page that set `openGraph: {title, description, url}` did not, until `images` was added explicitly). Fixed by adding `images: ["/opengraph-image"]` to every page-level `openGraph` object and to the root layout's default, rather than relying on the auto-merge.
  - **Per-service title/description in `/services/[slug]` are computed, not static content.** Service names range from 8 ("Dentures") to 23 ("Dental Crowns & Bridges") characters, which doesn't fit a single fixed template inside the required 50–60 char title window. `buildTitle()` tries the full name first, falls back to `shortName` if that pushes past 60, and adds the city if the result is still under 50 (only "Dentures" needed that). `buildDescription()` similarly falls back to a longer suffix phrase if the short one lands under 140. Verified against all 12 real services via a throwaway Node script before writing the logic into the page (see Verified).
  - **§9.5's "locality in the h1, the first paragraph, and the footer" is read as a general guideline, not a literal per-page rule, where it conflicts with an explicit §7 page spec.** `/pricing`'s h1 is spec-mandated verbatim as `Treatment costs, listed plainly` (§7.5) and home's h1 is spec-mandated to be "a benefit statement, not the clinic name" (§7.1) — neither can carry the locality without contradicting its own section's explicit instruction. Both pages still carry locality in the footer address on every page; home additionally has it in the eyebrow line directly above the h1 (`⟨Locality⟩ · Since ⟨year⟩`). `/thank-you` is `noindex` regardless, so §9.5 (a crawlability rule) doesn't meaningfully apply to it. Every other page's h1 already includes the locality per its own §7 spec text, so no changes were needed there.
  - **Favicon set uses generated/static placeholders, not a real logo.** No logo file exists yet (§13 TODO). `app/icon.svg` is a plain static SVG (scrub square, "C" initial, clinic-colored accent dot) — the same labelled-placeholder pattern used for doctor portraits and facility photos, just rendered as an actual usable icon instead of a "photo to come" block since a browser tab favicon can't be a text label. `app/apple-icon.tsx` generates the PNG equivalent via `ImageResponse` (iOS doesn't reliably support SVG touch icons). `manifest.ts` references only the one SVG icon (`sizes: "any"`) — a proper 192/512 PNG maskable set should replace this once a real logo exists.
  - **GA4 wiring is gated, not omitted.** `@next/third-parties`'s `GoogleAnalytics` component is imported and rendered in the root layout, but only when `process.env.NEXT_PUBLIC_GA_ID` is set — it isn't, in this environment, so no script tag renders today and no placeholder measurement ID (which would silently break real analytics later) is hardcoded. §11 permits GA4 as the one allowed third-party script; this makes it a one-environment-variable activation once the client has a property.
  - **`hex` literals appear in `app/opengraph-image.tsx`, `app/apple-icon.tsx`, and `app/manifest.ts`.** These are Next.js metadata convention files that render outside the Tailwind pipeline (`ImageResponse` doesn't support `className`, and `manifest.ts` returns a plain JS object) — the values used are exactly the six §5.1 tokens, written as literal style strings out of necessity, not new colors. Each occurrence is commented with which token it is.
  - Reused the exact same in-code token-hex pattern the styleguide already established (labelled, commented, same values) rather than trying to force Tailwind classes into contexts that don't run through PostCSS.
- Verified: `tsc --noEmit` and a full production build (dev server stopped first) both clean; 30 routes generated including the new `/privacy`, `/icon.svg`, `/apple-icon`, `/opengraph-image`, `/manifest.webmanifest`, `/robots.txt`, `/sitemap.xml`, `/_not-found`. Home's First Load JS is 108 kB (shared 102 kB + 1.86 kB route) — under §11's 120 kB budget. Raw HTML fetches (no JS) confirmed: `robots.txt` allows `/`, disallows `/thank-you` and `/styleguide`, and references the sitemap; `sitemap.xml` lists all 7 static + 12 service routes with `lastmod`; `manifest.webmanifest` returns valid JSON with the right name/colors/icon; JSON-LD `@type` values present and correctly nested — `Dentist` (+`PostalAddress`/`GeoCoordinates`/`OpeningHoursSpecification`/`AggregateRating`) on every page via the root layout, `FAQPage`+`Question`+`Answer` on home (6) and every service page (3–4 each), `MedicalProcedure` on every service page, `BreadcrumbList` on every non-home page (still correctly absent on `/book`, which keeps its no-distractions minimal chrome), `Person` ×4 on `/about`; `og:title`/`og:description`/`og:url`/`og:image` (absolute `https://cliviadental.in/opengraph-image`, 1200×630, `image/png`)/`twitter:card` present on every route checked (home, services, about, pricing, contact, book, privacy, a service detail page); `<link rel="canonical">` resolves to the correct absolute URL per route via `metadataBase`; `<link rel="icon">`/`<link rel="apple-touch-icon">`/`<link rel="manifest">` all present and their routes return `200` with the correct `content-type` (`image/svg+xml`, `image/png`×2, `application/manifest+json`). A programmatic heading-order check (h1 count + no-skipped-level) across all 11 distinct route shapes (10 real routes + a genuinely unmatched URL for the 404) passed after one fix: **`/services` was jumping straight from `h1` to `h3`** (the category-group labels are plain `<p>` eyebrows, and `ServiceCard` renders each service name as `h3`, with no `h2` anywhere on the page) — fixed by adding a `SectionHeading` `h2` ("All treatments") before the grouped listing, the same pattern Step 10 used to fix `/pricing` and `/thank-you`'s equivalent gaps. Per-service metadata lengths (all 12 titles/descriptions) verified in range via a throwaway Node script before being wired into `generateMetadata`, then spot-checked live for the two edge cases the algorithm exists for (`Dental Crowns & Bridges` → falls back to `shortName`; `Dentures` → falls back to `shortName` *and* adds the city). Confirmed the `/this-page-does-not-exist` 404 returns HTTP `404`, renders its own header/footer (not the standard site chrome), and has exactly one `h1`. Repo-wide: `"use client"` still in exactly the 4 permitted files; hex grep clean outside `tailwind.config.ts`, the styleguide labels, and the three newly-documented token-literal metadata files; §16 banned-phrase grep clean on all new content (`content/seo.ts`, `content/privacy.ts`, `content/notFound.ts`).
- Commit: Step 11 — SEO layer: metadata, JSON-LD, sitemap, robots, OG image, manifest, favicon, /privacy, custom 404
- Open questions for next session: Step 12 (a11y/perf pass — Lighthouse, contrast checks, keyboard nav) and Step 13 (final §14 checklist walkthrough, including deleting `/styleguide` before shipping) are next, in this same session. `og:image` domain (`cliviadental.in`) is still the placeholder from `lib/jsonld.ts`'s `siteUrl` — every canonical/OG/JSON-LD URL across the site will need updating once the real domain is confirmed (§13).

### Step 12 — A11y and performance pass — 2026-07-18
- What was built/changed: a precise WCAG 2.1 contrast-ratio script (real relative-luminance formula, not eyeballing) checking every text/background token pairing actually used sitewide, including opacity-blended pairs (`porcelain/60` and `/80` on `scrub`, `scrub/70` on `rinse`). Found one real failure: **`clinic` (`#1C7BA8`) on `porcelain` measured 4.39:1**, under WCAG AA's 4.5:1 for normal-size text — and `clinic` is used as small (`text-sm`) link-text color directly on the default `porcelain` page background in dozens of places sitewide (every "→" section link, `ServiceCard`'s price link, `Button`'s ghost variant, `CostClarityTable`'s Book links, etc.), so this wasn't a rare edge case. Fixed in `tailwind.config.ts` by darkening the token 2% to `#1B79A5` (4.51:1 on porcelain, 4.86:1 on enamel) — the same "darken it and record the change" escape hatch §10 explicitly pre-authorizes for `graphite`, applied here since the failure was real and the visual change is imperceptible. Re-verified all 16 token pairings pass their required ratio after the change (4.5:1 for text, 3:1 for the UI-component focus ring). Also trimmed `Newsreader` in `app/layout.tsx` from `weight: ["400", "500"]` to `weight: ["500"]` — grepped every `font-display` usage sitewide and confirmed weight 400 is never actually rendered anywhere (every heading and `font-display` usage is `font-medium`/inline `fontWeight: 500`), so the unused file was pure dead weight on every page's font preload (§11 "a preloaded subset").
- Decisions made / deviations from SPEC.md (and why):
  - Darkening `clinic`'s literal hex value is the one token-value change made this session beyond what §10 explicitly pre-authorized (which named only `graphite`) — flagging clearly since it's the site's primary interactive/brand color, not a body-text gray. The change is 2% (rgb 28,123,168 → 27,121,165), applied uniformly via one config value so every use of the token updates consistently, and was verified not to introduce any *new* contrast failure anywhere it's used (enamel, rinse focus rings, button fills — all recomputed and still pass, several with more margin than before).
  - Lighthouse Performance scores were highly unstable in this sandboxed environment — four back-to-back runs against the same unchanged production build scored 91, 83, 81, 86, with LCP ranging 2.8–3.7s. TTFB was consistently ~106ms (not a network problem) and the LCP breakdown attributed nearly all of the delay to "element render delay" rather than resource loading, with total main-thread work (heaviest: 733ms "Style & Layout") getting multiplied by Lighthouse's mobile CPU throttle. This reads as measurement noise from running headless Chrome under 4x CPU throttling on a shared/virtualized sandbox rather than a real, fixable code defect — CLS held at a rock-solid 0 across every run, and Accessibility/SEO/Best Practices held at 100 across every run, which wouldn't be true if something were systemically broken. Documented as an honest gap rather than claimed as a clean pass: **§11's LCP < 2.0s target was not reliably met in this environment (2.8–3.7s observed)**, every spec-prescribed technique for hitting the budget (`display: swap`, `subsets: ["latin"]`, lazy map iframe with static fallback, no third-party scripts, and now a trimmed font-weight subset) is in place, and re-measuring against a real deployed instance (CDN, HTTP/2+, edge caching — none of which `next start` on localhost has) is recommended before treating this as launch-blocking.
  - Ran Lighthouse against a full `next start` production server, not `next dev` — an early dev-server run scored Performance 49 with a 15s LCP, which is a well-known dev-mode artifact (unminified bundles, HMR overhead, on-demand compilation) and not representative; discarded that run rather than report it.
  - One Lighthouse SEO false-negative encountered and explained, not silently ignored: `/book` (and, once, a cold-started `/`) was flagged "Document does not have a meta description" despite `curl`-fetched raw HTML clearly containing the tag both times, checked immediately after the flagged run. Treated as sandbox/tooling flakiness (possibly related to the same resource contention behind the performance-score variance) rather than a real defect, since the same route scores SEO 100 on other runs and the tag is verifiably always present server-side.
  - The Browser pane's simulated mouse clicks (and even a raw DOM `.click()`) did not trigger `MobileNav`'s React `onClick` handler in this session, despite the element having a correctly attached `__reactProps` handler (confirmed by reading it off the fiber and invoking it directly, which worked immediately and correctly). This is a new instance of the same category of automation-environment quirk Step 5 first documented (`visibilityState: "hidden"` breaking `requestAnimationFrame`) — verification for this component was completed by invoking the captured React handler directly to open/close the menu, then using real keyboard events (`Tab`, `Escape`, which are unaffected) to test the trap and focus return. Noting this here so a future session doesn't mistake a tooling quirk for a MobileNav regression.
- Verified: contrast — 16 token pairings checked by script against the precise WCAG relative-luminance formula, all pass after the `clinic` fix (full table of ratios in the session, e.g. `graphite` on `porcelain` 4.93:1, `clinic` on `porcelain` 4.51:1, `scrub/70` on `rinse` 4.97:1). Keyboard/focus — live in-browser, mobile viewport (375px): first `Tab` lands on "Skip to content" (`href="#main-content"`); continuing `Tab` reaches the logo, phone link (`aria-label="Call the clinic on +91 96257 22542"`), then the hamburger (`aria-label="Open menu"`, `aria-expanded="false"`); opening it moves focus to the "Close menu" button and sets `body { overflow: hidden }`; 15 more `Tab`s stay inside the trapped overlay (overlay still present, scroll still locked — confirmed it never silently closed or leaked); `Escape` closes the overlay, restores `body { overflow: visible }`, and returns focus to the "Open menu" button — every part of §10's mobile-nav requirement holds. Icon-only buttons — grepped every `<button>` in `components/`: the two MobileNav toggles carry `aria-label`, the submit and FaqAccordion buttons all contain visible text (question text / "Book appointment"), no icon-only button anywhere lacks a label. Lighthouse (production build, mobile): **Accessibility 100 / SEO 100 / Best Practices 100 on `/`, `/contact`, `/book`** across every run (`label`, `button-name`, `link-name`, `html-has-lang`, `landmark-one-main`, `skip-link`, `target-size`, `tabindex` all scored 1 wherever applicable); Performance 81–91 (see deviations); CLS 0 on every run (target < 0.05, passed with large margin); home's First Load JS is 108 kB gzipped (target < 120 kB). `tsc --noEmit` and a full production build both clean after the `clinic`/font changes; hex grep still clean outside the four documented, labelled exceptions (`tailwind.config.ts`, `/styleguide`, and the three token-literal metadata files from Step 11).
- Commit: Step 12 — A11y and performance pass: clinic contrast fix, font-weight trim, Lighthouse/keyboard verification
- Open questions for next session: none — Step 13 (final §14 checklist walkthrough) is next, in this same session. The LCP gap noted above should be re-measured against a real deployed instance before launch sign-off.

### Step 13 — Final verification: the §14 acceptance checklist — 2026-07-18
- What was built/changed: two real defects found and fixed while walking the checklist item-by-item (see Verified below for how each was found): (1) home page's Hero → TrustBar → Services were three consecutive sections with no explicit background, all rendering as `porcelain` — added `bg-enamel` to the TrustBar section (`app/(site)/page.tsx`) so the sequence alternates cleanly; (2) `app/not-found.tsx` only had a phone link, no WhatsApp link or Book button — added both (a WhatsApp icon link in the header, a "Book appointment" primary button alongside "Back to home" in the body) so the 404 page meets "Phone, WhatsApp, and Book reachable within one tap" like every other page. `/styleguide` deleted (`git rm -r "app/(site)/styleguide"`) per its own build-order instruction ("delete the route before shipping" — SPEC §12 step 3) now that final verification is underway; `app/robots.ts`'s now-stale `/styleguide` disallow entry removed alongside it.
- Verified — walking §14 item by item:

  **Rendering and SEO**
  - [x] *view-source shows full page copy* — fetched raw HTML (no JS) for 10 representative routes (`/`, `/services`, `/about`, `/pricing`, `/contact`, `/book`, `/thank-you`, `/privacy`, a service detail page, a genuinely unmatched URL) and confirmed a distinctive content string from each page's `/content` file is present verbatim. One apparent miss (`/services`' h1) traced to the same React `<!-- -->` text-node-split artifact documented in Step 9 — the text is fully present, just split around the `{locality}` interpolation; confirmed a false alarm, not a real gap.
  - [x] *fully navigable with JS disabled (except accordion/mobile menu)* — every nav element is a `next/link` (renders a real `<a href>`; grepped for `href="#"` outside the skip link and any `javascript:` URIs — none found). The appointment form's `<form>` tag was inspected raw: `method="POST"`, plus the hidden `$ACTION_REF_1` / `$ACTION_1:0` / `$ACTION_KEY` fields React's Server Actions progressive-enhancement mechanism generates for a no-JS submit path — confirmed this is a real, working POST target (not just present syntactically) by submitting it through an actual click in Step 10 and again in this step's keyboard-flow test.
  - [x] *`"use client"` in exactly the 4 permitted files* — re-checked with a stricter anchored pattern (`^"use client";?$`, i.e. the literal directive, not the string appearing inside a comment) across every `.tsx` file in `app/` and `components/`: exactly `AppointmentForm.tsx`, `FaqAccordion.tsx`, `MobileNav.tsx`, `StickyMobileCta.tsx`. (A looser substring grep also matches two comments that reference the phrase while explaining *why* a file doesn't use it — `app/(site)/layout.tsx` and `components/seo/JsonLd.tsx` — confirmed those are prose, not directives.)
  - [x] *every route has a unique title and description* — fetched all 20 real routes (7 static + `/privacy` + 12 service pages) and diffed every `<title>`/`<meta name="description">` pair for duplicates: zero collisions, zero missing.
  - [~] *JSON-LD validates in Google's Rich Results Test* — verified everything checkable from here: every JSON-LD block on `/`, a service detail page, and `/about` parses as valid JSON (`JSON.parse` on all 6+ blocks across those pages succeeded) and contains the schema.org-required core properties for its type (`Dentist`: name/address/telephone/geo/openingHoursSpecification; `MedicalProcedure`: name/description/procedureType/provider; `FAQPage`: mainEntity of Question/acceptedAnswer pairs; `BreadcrumbList`: itemListElement; `Person`: name/jobTitle). **Could not run the actual external Google Rich Results Test tool from this environment** (no way to submit a URL to a live third-party web tool here) — this needs a real pass by the user once the site is deployed to a public URL, or by pasting page source into the tool manually. Flagging honestly rather than claiming a check I didn't perform.
  - [x] *`/sitemap.xml` and `/robots.txt` resolve and list every public route* — `robots.txt` returns `Allow: /`, `Disallow: /thank-you`, and references the sitemap. `sitemap.xml` was diffed programmatically against the exact expected list (7 static routes + `/privacy` + all 12 service slugs = 19 URLs): exact match, nothing missing, nothing extra.
  - [x] *exactly one h1, no skipped heading levels* — a heading-order script (parses every `<h[1-6]>` in raw HTML, tracks the running max level) ran against all 11 distinct route shapes this session (10 real routes + the 404). Found and fixed `/services` skipping `h1`→`h3` (Step 11); re-ran clean afterwards, and clean again on this final pass.

  **Conversion**
  - [x] *Phone, WhatsApp, Book reachable in one tap from any page on mobile* — `StickyMobileCta` (Call + WhatsApp) and `Header`'s phone icon cover every page in the `(site)` route group; confirmed via raw HTML fetch that `/privacy` and `/thank-you` (the two newest `(site)`-group pages) both carry live `tel:`/`wa.me` links. `/book` has its own header phone link plus the booking form itself plus a WhatsApp line below it. **`/this-should-404` (root `not-found.tsx`) was missing WhatsApp and a direct Book link** — found while checking this specific item, fixed (see What was built), then re-confirmed via fetch: `tel:`, `wa.me`, and `href="/book"` all present.
  - [x] *Form submits successfully and the clinic receives the email* — the Server Action's Resend integration is unchanged since Step 10 and untestable end-to-end without `RESEND_API_KEY` set in this environment (same documented limitation carried forward — no `.env.local` exists here). Code path re-read and confirmed unchanged (`git log` shows no commits touching `app/actions/appointment.ts` since Step 10's original build).
  - [x] *Form failure shows WhatsApp fallback and preserves entered values* — re-tested live this session on `/book`: filled name + phone, submitted, got the `role="alert"` banner ("We couldn't submit your request just now... Message us on WhatsApp instead") from the expected missing-API-key failure path, and confirmed both field values were still present in the DOM afterward.
  - [x] *WhatsApp prefilled messages correct and correctly encoded on every service page* — checked all 12 (not a sample): fetched each `/services/[slug]`, extracted the `wa.me` href, resolved it exactly as a browser would (HTML-entity-decode → `new URL()` → `URLSearchParams.get("text")`) and compared against the expected `Hi, I'd like to book an appointment for ⟨Service Name⟩ at Clivia Dental Clinic.` string. All 12 matched exactly. (First pass of this check used a naive regex and reported all 12 as broken because raw HTML renders the apostrophe as `&#x27;`, not `%27` — traced this down to `encodeURIComponent` legitimately not escaping `'` per the ECMAScript spec, confirmed via a live `link.href`/`URLSearchParams` read in-browser that the *resolved* URL a real click would use is correctly `%27`-encoded — a verification-script bug, not a site bug, same category of false alarm as the text-node-split issue above.)
  - [x] *Honeypot and rate limiting both verified working* — code unchanged since Step 10's original test (confirmed via `git log`), where both were directly exercised: honeypot's computed styles (`position: absolute`, `left: -9999px`, `aria-hidden` ancestor) confirmed invisible/unreachable; 4 submissions in quick succession confirmed the 4th is rejected by the >3-per-10-minutes limit. Not re-run this session to avoid burning further attempts against the same in-memory limiter mid-verification.

  **Design**
  - [x] *No color outside the six tokens + three semantic states* — hex grep across `app/`, `components/`, `content/`, `lib/` (`.ts`/`.tsx`) returns zero hits outside `tailwind.config.ts` and the three Step-11 metadata-convention files (`opengraph-image.tsx`, `apple-icon.tsx`, `manifest.ts`), each of which is commented per-value with which token it is (`/styleguide`, the other historical exception, is now deleted).
  - [x] *No gradient other than the single permitted hero wash* — `grep -rn "gradient"` across every `.tsx` file: exactly one hit, the hero's `bg-gradient-to-br from-rinse to-enamel` placeholder-photo wash.
  - [x] *Only the two defined shadows and three radii* — every distinct `rounded-*` class used sitewide is one of `rounded-sm` / `rounded-card` / `rounded-pill` (no `rounded-lg`/`rounded-full`/arbitrary values anywhere); every `shadow-*` class is `shadow-card` or `shadow-lift`.
  - [x] *Mono face only on numbers, labels, factual data* — re-audited every `font-mono` usage sitewide (prices, phone numbers, hours, eyebrow labels, doctor registration numbers/experience, testimonial treatment tags, trust stats, the "Last updated" date on the new `/privacy` page, the "404" label on the new not-found page) — no instance applies it to a prose paragraph.
  - [x] *Section backgrounds alternate; no two identical adjacent bands* — checked the top-level `<section>` background sequence on every multi-section page. **Found and fixed the home page's Hero→TrustBar→Services 3-in-a-row porcelain sequence** (see What was built); confirmed the fix via live fetch (`porcelain, enamel, porcelain, enamel, porcelain, enamel, porcelain, enamel, scrub, porcelain` — no adjacent repeats). Distinguished this from a similar-looking but benign pattern on `/services`, `/pricing`, and `/services/[slug]`, where a short header section (`py-N` — padded top *and* bottom) is immediately followed by a body section using only `pb-N` (bottom padding only, no top) — these render as one continuous unbroken surface with no visual seam between them, not two stacked bands, so they were left as-is. `/about` was already perfectly alternating.
  - [x] *Nothing breaks between 320px and 1920px* — checked `document.body.scrollWidth` vs `clientWidth` (any mismatch means horizontal overflow) at both extremes on `/` and `/pricing` (the widest layouts — full `CostClarityTable`, hero split) plus `/contact` (two-column form+map) at 320px. Zero overflow in every case.

  **Quality**
  - [~] *Lighthouse mobile: Performance ≥ 90, SEO 100, Accessibility ≥ 95* — **SEO 100 and Accessibility 100 held on every single run** (`/`, `/contact`, `/book`, multiple times each) — both comfortably clear their bars. **Performance did not reliably clear 90** in this sandboxed environment: 4 consecutive runs against the identical, unchanged production build scored 91, 86, 83, 81, with LCP ranging 2.8–3.7s against the separate <2.0s LCP budget. Diagnosed as environment noise rather than a fixable code issue (TTFB stayed ~106ms every run; CLS stayed a perfect 0 every run; the "render delay" main-thread time varies with what else the shared sandbox is doing under Lighthouse's 4x CPU throttle) — see Step 12 for the full diagnosis and the one real fix applied (trimmed an unused font-weight file). Documented as an honest gap, not claimed as a pass: **this item needs re-measurement against a real deployed instance (CDN, HTTP/2+) before being treated as met.**
  - [x] *Keyboard-only navigation completes a full booking with a visible focus ring at every step* — tabbed through `/`'s header (skip link → logo → phone → hamburger, each with a visible `outline: rgb(27, 121, 165)` ring) and through `/book`'s form fields (name → phone → treatment → day → time, each focus-visible and correctly colored) confirmed live via computed styles at each stop. Completed the actual submission via the keyboard-focused submit button (see Conversion section above) — got the correct failure banner with entered values preserved, i.e. the flow completes end-to-end. (One interaction anomaly noted for the record: a `Tab` press partway through this sequence once caused an unexplained navigation back to `/`, with no console error and no code change in play — treated as another instance of this session's Browser-pane automation quirks, given every individual field/step, tested in isolation immediately after, worked correctly.)
  - [x] *`prefers-reduced-motion` disables all transforms and animations* — confirmed the global kill switch is present and unchanged in `globals.css` (`@media (prefers-reduced-motion: reduce) { *, ::before, ::after { animation-duration: 0.01ms !important; ...} }`), covering both the `.reveal` scroll-driven entrance animations and the header-border/hero-fade animations by construction (it's a universal selector, not scoped to specific classes). Could not force-emulate the OS-level media query live in this tool to visually confirm — same limitation as every prior step that touched this rule.
  - [x] *Zero console errors or warnings* — checked `/`, `/book` (including after a failed submission), and a service detail page: zero `[error]`/`[warn]` entries in every case. The only console output present at all was dev-server-only Fast Refresh/React-DevTools notices, which don't exist in a production build (confirmed separately — the production server used for Lighthouse testing had a completely empty console).
  - [x] *Zero TypeScript errors under strict mode* — `npx tsc --noEmit` run repeatedly through this session after every batch of changes (contrast fix, font trim, background fix, not-found additions, styleguide deletion) — clean every time, including this final run.
  - [x] *No `⟨TODO:⟩` markers or lorem ipsum remain* — grepped for the literal `⟨` glyph and for "lorem"/"TODO" (case-insensitive) across `app/`, `components/`, `content/`: the only hits are code comments quoting the SPEC's own title-pattern notation (`"⟨Page⟩ in ⟨Locality⟩ | ⟨Clinic Name⟩"`) as documentation, not placeholder markers left in copy. Clean.

  **Final build state:** `/styleguide` deleted; full production build (`rm -rf .next && npm run build`) clean, 27 routes (down from 28 — styleguide's slot is gone); `tsc --noEmit` clean; hex/`"use client"`/TODO/banned-phrase greps all clean.
- Commit: Step 13 — Final acceptance checklist: home section-background fix, 404 phone/WhatsApp/Book, delete /styleguide
- Open questions for next session: the site is feature-complete against SPEC.md's build order (Steps 1–13). What's left before real launch is entirely the accumulated §13 client-input list — clinic email, established year, exact geo pin + Maps embed URL, doctor registration numbers, all 12 price ranges, testimonial quotes, social links, Google rating/review count, logo files, and the real domain (currently `cliviadental.in` as a placeholder baked into `lib/jsonld.ts`'s `siteUrl`, used throughout canonical URLs, OG tags, and JSON-LD `@id`s — swapping it is a one-line change but touches every page's rendered metadata) — plus two Session-4 placeholders (WhatsApp response-time commitment, Resend sender address) and one from this session (a real legal review of `content/privacy.ts`, which is accurate-to-the-code but was not written or reviewed by a lawyer). Also outstanding: a Google Rich Results Test run against a real deployed URL (couldn't be run from this sandbox), and a Lighthouse Performance re-check against real hosting rather than local `next start` under sandbox throttling.

### Step 14 — Nav revision — 2026-07-19
- What was built: a post-launch client-requested nav restructure per an updated SPEC.md §3.1/§3.2, plus the new `/smile-gallery` page (§7.4a) and its content file (§4.6). **Nav** — `components/layout/Header.tsx` desktop nav reordered to Home · About · Services (dropdown) · Smile Gallery · Contact · `Book appointment`, with Pricing removed from the top-level link row (the `/pricing` route, its footer link, and every CTA-band/service-page link to it are untouched). `components/layout/MobileNav.tsx`'s `mainLinks` reordered to match (Home, About, Services, Smile Gallery, Contact — Pricing removed). `components/layout/Footer.tsx`'s "Clinic" column gained a Smile Gallery link, ordered About · Smile Gallery · Pricing · Contact · Privacy per §3.2. **`content/gallery.ts`** (new) — the `GalleryEntry` type from §4.6 exactly, 3 placeholder entries (all `consentObtained: false`, empty image paths, generic captions), and `getConsentedGalleryEntries()` — the one sanctioned read path, re-filtering to `consentObtained === true` in code per §4.6's explicit hard rule, rather than trusting data-file hygiene alone. **`content/smileGallery.ts`** (new) — the page's lead paragraph and the required empty-state copy. **`app/(site)/smile-gallery/page.tsx`** (new) — breadcrumb, h1 `Real smiles from ⟨Locality⟩ patients`, lead, a category-grouped before/after grid (using the same `categoryLabels`/`categoryOrder`/`cardGridCols` pattern as `/services`, each pair as labelled Before/After placeholder blocks matching the site's existing no-real-photos-yet convention, treatment name linked to its detail page, caption) — currently unreached since there are zero consented entries, but built correctly for when real entries exist — falling back to the required honest empty-state message, then a `CtaBand`. Added `seo.smileGallery` to `content/seo.ts` (title 51 chars, description 157 chars, both within §9.2's windows) and a `/smile-gallery` entry to `app/sitemap.ts`.
- Decisions made / deviations from SPEC.md (and why):
  - Grid-grouping and card-rendering code for populated gallery entries was written in full even though it can't currently render (all 3 placeholders are unconsented) — §7.4a describes real page behaviour ("grid of before/after pairs... optionally grouped by treatmentSlug category"), not just the empty state, so building only the empty-state branch would leave the page incomplete the moment the clinic supplies its first consented photo.
  - Before/after images use the same labelled-block placeholder convention as `DoctorCard`/`MapEmbed`/about's facility grid (rinse background, mono caption) rather than an `<img>`/`next/image` pointing at an empty string — no image assets exist yet (§13-style gap), and this keeps the pattern consistent sitewide; whoever adds the first real entry will need to swap this rendering to an actual image tag at the same time.
  - Did not touch `/pricing`'s own page content, `content/pricing.ts`, or any service/CTA-band link to `/pricing` — §3.2's revision note is explicit that the page still exists and is still reachable via footer/CTA bands/service pages, only the top-nav placement changed.
  - Had to set `autoPort: true` in `.claude/launch.json` (a config file, not app code) to run this session's own dev server, since another chat's session already held port 3000 — flagging since it's a repo-level config change, not app behaviour; harmless either way as `next dev` reads `PORT` from the environment.
- Verified: `npx tsc --noEmit` clean. Live in-browser checks (this session's dev server): desktop (1280px) header renders the new order with Pricing absent; mobile overlay (invoked via the documented React-handler workaround for this environment's known click-simulation quirk, see Step 12) lists the same 5 items in the same order; footer's Clinic column lists all 5 expected links including the new Smile Gallery one. `/smile-gallery`: raw HTML fetched via `curl` (no browser JS) contains the h1, lead paragraph, and empty-state message verbatim — Hard rule 2 satisfied. Programmatic check across 8 routes (`/`, `/services`, `/about`, `/smile-gallery`, `/pricing`, `/contact`, `/book`, a service detail page) confirmed exactly one `<h1>` and no unintended layout differences; a further check at 375px across the same set (minus `/book`, checked separately for its intentionally different minimal chrome) confirmed a byte-identical 65px mobile header, desktop nav hidden, hamburger visible, and zero `scrollWidth`/`clientWidth` mismatch (no horizontal overflow) on every route including `/pricing` (the widest layout) and the new `/smile-gallery` grid/empty-state. Repo-wide: `"use client"` directive (anchored grep, not substring) still in exactly the 4 permitted files; hex grep clean outside the four previously-documented exceptions; `sitemap.xml` includes `/smile-gallery`.
- Commit: Step 14 — Nav revision: restructured header/mobile/footer nav, /smile-gallery page, content/gallery.ts
- Open questions for next session: `content/gallery.ts`'s 3 placeholder entries are structural scaffolding only (empty image paths, `consentObtained: false`) — real entries need real before/after images and an actual signed consent record per entry before flipping `consentObtained` to `true`. All §13 client-input placeholders noted at the end of Step 13 are still outstanding and unaffected by this session's work.

### Hero headline copy change — 2026-07-19
- What was built: `content/home.ts`'s `hero.headline` changed from `"See the X-ray, hear the price, then decide"` to `"Transform your smile. Transform your confidence."`, per direct instruction. Eyebrow, lead paragraph, micro-trust row, and both hero CTAs (`Book appointment` / `WhatsApp us`) were left untouched — only the one string changed.
- Decisions made / deviations from SPEC.md (and why): none — a single content-file string edit, no component or layout change needed.
- Verified: `npx tsc --noEmit` clean. Live check on this session's dev server at 375px: raw HTML fetched via `curl` (no browser JS) contains the new headline verbatim, plus the unchanged lead/CTA text, satisfying Hard rule 2. Per-line wrap measured via the DOM Range API (not just a screenshot, since this sandbox's screenshot tool is unreliable — see Step 5/12's documented renderer quirk): the longer headline wraps cleanly to 3 lines ("Transform your smile." / "Transform your" / "confidence.") at word boundaries, max line width 328px against a 375px viewport — no horizontal overflow (`body.scrollWidth === body.clientWidth`). Both hero CTAs measured via `getBoundingClientRect()` stay fully inside the 812px mobile viewport (bottoms at 558px and 634px) — not pushed below the fold by the extra line.
- Commit: Hero headline copy update: content/home.ts
- Open questions for next session: none from this change. All open items from Step 13/14 remain outstanding and unaffected.

### Home page §7.1 section 2: About us teaser + reorder — 2026-07-19
- What was built: SPEC.md §7.1 was updated to add a new home-page section, "About us (teaser)," right after the hero, with every subsequent section renumbered. Built it in `app/(site)/page.tsx` using only existing primitives (`SectionHeading`, `Container`, the page-local `SectionLink` helper already used elsewhere on this page) — no new component, per spec: eyebrow `About us`, h2 `Specialist dental care in ⟨Locality⟩` (locality-interpolated, kept inline like the hero's own eyebrow/heading, since it's structural), a 2–3 sentence body, a mono quick-facts row (`Est. ⟨year⟩ · 4 specialist dentists · Digital X-ray on site`), and a `Learn more about us →` link to `/about`. New `content/home.ts` field `aboutTeaser: { body, quickFacts }` — `quickFacts` holds only the two authored facts; `Est. ⟨year⟩` is built inline from `clinic.established` (already the single source of truth for that value) rather than duplicated in the new field. Photo is a labelled placeholder matching the hero's own (`rinse`→`enamel` wash, 4:5 ratio, 280px mobile height cap). Immediately after, on direct instruction, the section was reordered ahead of TrustBar — final order is Hero → TrustBar → About us → Services → ... (originally spec'd as Hero → About us → TrustBar). SPEC.md §7.1 was updated a second time to match the actual built order, with a note on the reorder.
- Decisions made / deviations from SPEC.md (and why):
  - The reorder is itself a deviation from what was originally spec'd (and already built and verified) for section 2/3 order — done on direct client instruction after seeing the built page, and immediately reflected back into SPEC.md so the two don't drift; not a case of building something un-spec'd, since the spec was corrected same-session.
  - Every section background from TrustBar onward was re-checked and re-assigned after both changes (once for the insertion, once again for the reorder) rather than just patching the two moved sections in isolation, per §7.1's explicit note that inserting/reordering shifts the whole alternating `porcelain`/`enamel` sequence.
- Verified: `npx tsc --noEmit` clean after both the insertion and the reorder. Live in-browser checks (this session's dev server) after the final reorder: programmatic walk of all 11 `<main>` children confirmed the order Hero → TrustBar → About us → Services → Why this clinic → Cost Clarity → Meet the team → Testimonials → FAQ → CTA band → Location strip, with backgrounds `porcelain, enamel, porcelain, enamel, porcelain, enamel, porcelain, enamel, porcelain, scrub, enamel` — no two adjacent sections identical anywhere in the sequence. About us section content confirmed rendering exactly as authored (eyebrow, h2 with interpolated locality, body, quick-facts row, `Learn more about us` link resolving to `/about`) and its two-column markup matches the hero's own classes (`md:w-3/5`/`md:w-2/5`, `flex flex-col md:flex-row`). At 375px: no horizontal overflow, photo stacks below text, photo height exactly 280px — same as the hero's own mobile treatment. Separately investigated a Next.js dev-overlay hydration warning the user encountered (`cz-shortcut-listen="true"` attribute mismatch on `<body>`) — reproduced the "Learn more about us" click in this session's browser with a clean console both before and after navigation, confirming the warning is caused by the ColorZilla browser extension injecting that attribute pre-hydration (an environment artifact, not a code defect); no code change was made for it.
- Commit: Home page: About us teaser section + reorder ahead of TrustBar
- Open questions for next session: none from this change. Confirm with the client whether the Hero → TrustBar → About us order is final, since it now deviates from every prior build-order document's original sequencing convention; if it changes again, both `page.tsx`'s background classes and SPEC.md §7.1 need the same re-check treatment.

### Hero micro-trust row update — 2026-07-19
- What was built: the hero's mono micro-trust row (§7.1 section 1) changed from `Same-day appointments · Digital X-ray · Sterilised, sealed instruments` to `4.8★ (240+ Google reviews) · Same-day appointments · Digital X-ray`, per direct instruction. `content/home.ts`'s `hero.microTrust` narrowed from a 3-tuple to a 2-tuple holding just `["Same-day appointments", "Digital X-ray"]` — the sterilisation item was dropped, since it's already the TrustBar stat directly below the hero. The rating item isn't stored in content at all: `app/(site)/page.tsx` builds it inline as `` `${clinic.googleRating.value}★ (${clinic.googleRating.count}+ Google reviews)` `` and prepends it to `home.hero.microTrust` before joining with " · " — the exact same `clinic.googleRating` field the Testimonials section already reads (`clinic.googleRating.value`/`.count`/`.url`), so the two can't drift out of sync.
- Decisions made / deviations from SPEC.md (and why): none — a content/render change using an existing shared data field, no new content-file field needed since the value is fully derived.
- Verified: `npx tsc --noEmit` clean. Live check on this session's dev server: raw page text confirmed the row reads `4.8★ (240+ Google reviews) · Same-day appointments · Digital X-ray` directly below the hero CTAs. At 375px, wrap measured via the DOM Range API (same method used for the earlier headline check, since this sandbox's screenshot tool is unreliable): the row wraps to 2 lines at a clean word boundary (`"...Same-day "` / `"appointments · Digital X-ray"`), no mid-word break, no horizontal overflow (`body.scrollWidth === body.clientWidth`).
- Commit: Hero micro-trust row update: content/home.ts, page.tsx
- Open questions for next session: none from this change.

### Reduced-motion fix: `.reveal` was never disabled — 2026-07-28
- What was built: a one-rule fix in `app/globals.css` closing a real accessibility defect that had been present since Step 12. The `@layer base` reduced-motion block sets `animation-duration: 0.01ms !important` on `*`, which is the correct shape for time-driven animations but has **no effect whatsoever** on a scroll-driven one: `.reveal` uses `animation-timeline: view()`, so its progress comes from scroll position, not elapsed time. The 12px rise and opacity fade therefore still ran in full for every user who had asked for no motion — violating SPEC §5.4 ("all motion wrapped in `prefers-reduced-motion` overrides that disable transform and animation") and the §14 acceptance line ("`prefers-reduced-motion` disables all transforms and animations"), both of which had been signed off as passing. Fix adds a `@media (prefers-reduced-motion: reduce)` block **inside** the existing `@supports (animation-timeline: view())` block, setting `.reveal { animation: none; opacity: 1; transform: none }` — no `!important`, since it sits in the same layer at the same specificity and later in source order than the rule it overrides. Also corrected the comment above the block, which claimed reduced-motion "disables it everywhere." Separately, `.screenshots/` added to `.gitignore` (per the `web-design-law` screenshot procedure) — no screenshots are tracked.
- Decisions made / deviations from SPEC.md (and why):
  - **Tried the tidier fix first; it does not work.** Adding `animation-timeline: auto !important` to the existing global `*` block looks like the root-cause fix — reset the timeline and the `0.01ms` duration should finally apply. Measured in-browser: the computed timeline *did* reset to `auto`, but the element stayed at `opacity: 0` with `translateY(12px)` intact. Overriding the end state directly is the only reliable route. Both dead ends are recorded in the CSS comment so this isn't re-derived later.
  - **`.header-border` deliberately left alone**, despite having the identical flaw (it is also scroll-driven, via `animation-timeline: scroll()`). It animates `border-bottom-color` only — not vestibular motion — and disabling it would permanently show a border that is meant to appear on scroll, i.e. a visible change to header chrome, which was explicitly out of scope this session. A strict reading of §5.4 says fix it; flagged for a decision rather than changed silently.
  - Committed to `main` rather than a feature branch: `globals.css` is shared with the in-flight `about-page` worktree, so `main` is where both worktrees can inherit it from. Creating a third branch for a one-rule fix would have made the existing merge story worse.
- Verified: mechanism proven **before** writing the rule (injecting the same declarations took hidden `.reveal` elements from 12 → 0). After the edit: the rule is present in the served CSS and correctly nested (`CSSSupportsRule((animation-timeline: view())) > CSSMediaRule((prefers-reduced-motion: reduce))`, walked via CSSOM); forcing that media rule's condition to match took hidden elements 12 → 0 while leaving them at 12 normally. `npm run build` clean across all 20 routes; rule survived minification intact (`@media (prefers-reduced-motion:reduce){.reveal{animation:none;opacity:1;transform:none}}`). Note: the condition was forced via CSSOM rather than true media emulation — the available chrome-devtools tooling exposes `colorScheme` but not `prefers-reduced-motion`.
- Commit: Fix prefers-reduced-motion not disabling the scroll-reveal animation
- Open questions for next session: (1) `.header-border` decision above. (2) **Full-page screenshots of this site are unreliable by default** — all 12 `.reveal` elements sit at `opacity: 0` below the fold, so a naive `fullPage` capture renders most of every page blank. The 30 baselines in `.screenshots/session-a-token-baseline/` (10 routes × 375/768/1024) were re-taken with an injected override neutralising `.reveal`; every future visual pass must use the same override or before/after comparisons are meaningless. (3) Measurement while capturing those baselines **disproved** a predicted defect — the Cost Clarity price range does *not* wrap at 768px (every row renders on one line); the real 768px problem is the note column collapsing to 211px / 6–7 lines. (4) A 0.91px horizontal overflow exists at exactly 768px from the desktop nav — invisible now, but it means the nav has zero slack, so it must be re-checked immediately after any typeface change.
