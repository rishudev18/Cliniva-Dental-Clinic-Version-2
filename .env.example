# Copy to .env.local and fill in for the appointment form to send email.
# Without this, the form still validates and rate-limits correctly, but
# submissions fail over to the WhatsApp-fallback error banner (SPEC §8.2).
RESEND_API_KEY=
